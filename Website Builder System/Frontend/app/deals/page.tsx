import DealsFeed from '@/components/deals/DealsFeed';

export const metadata = {
  title: 'Local Deals | Crossroads',
  description: 'Fresh offers from local businesses in Markham.',
};

export default function DealsPage() {
  return <DealsFeed />;
}
